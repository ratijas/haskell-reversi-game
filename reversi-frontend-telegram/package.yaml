name:                reversi-frontend-telegram
version:             0.1.0.0
github:              "k13andone/reversi-frontend-telegram"
license:             BSD3
author:              "richard feynman"
maintainer:          "pingme@gmail.com"
copyright:           "(c) (r) ™ &quot;[object Object]&quot;"

extra-source-files:
- README.md
- ChangeLog.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/k13andone/reversi-frontend-telegram#readme>

dependencies:
- base >= 4.7 && < 5

library:
  source-dirs: src

executables:
  reversi-frontend-telegram-exe:
    main:                Main.hs
    source-dirs:         app
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - reversi-frontend-telegram

tests:
  reversi-frontend-telegram-test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - reversi-frontend-telegram
